#include<iostream>
#define MAXN 6002
using namespace std;

int n;
int val[ MAXN ];

int child[ MAXN ][ MAXN ];
int p[ MAXN ];

void dfs(int x , bool chose)

int main()
{
  int i;
  int root = 0;
  cin>>n;
  for( i = 1 ; i <= n ; ++ i)
    cin>>val[ i ];
  for( i = 1 ; i <= n ; ++ i)
    {
      cin>>u>>v;
    }
  for(i = 1 ; i <= n ; ++ i)
    if(p[i] == 0)
      {root = i ; break;}
  cout<<max( dfs(root,false) , dfs(root,false) )<<"\n";
  return 0;
}
