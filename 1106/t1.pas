program input_test;
var
   a		 : array[ 1..10 ] of string[ 100 ];
   i , j , n , m : integer;
   k		 : integer;
begin
   
   {i := 1 ; 
   repeat
      read(a[ i ]);
      writeln( a[ i ] );
      inc( i );
      readln;
   until integer(a[ i - 1 ][ 1 ]) = 0;}
   repeat
      if readln(a[ i ]) then
	 
   until integer(a[1][1]) = 10;
   writeln('end');
end.