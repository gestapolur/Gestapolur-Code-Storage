#include<iostream>
using namespace std;

int a[ MAXN ] , b[ MAXN ];
int n,m;

int main()
{
  int i;
  cin>>n;
  for( i = 1 ; i <= n ; ++ i )
    {
      cin>>a[ i ];
      m = m > a[ i ] ? m : a[ i ];
    }
  //bucket sort
  return 0;
}
