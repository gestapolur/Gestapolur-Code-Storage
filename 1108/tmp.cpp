#include<iostream>
#define MAXN 100000
using namespace std;

int n , pos ;
double spd;
int f[ MAXN ][ MAXN ];

int main()
{
  int i;
  while( cin>>n and n not_eq 0 )
    {
      cin>>spd>>pos;
      for( i = 1 ; i <= n ; ++ i )
	cin>>p[ i ]>>ed[ i ];
      sort( 1 , n );
      for( i = 1 ; i <= pos ; ++ i )
	for( j = pos ; j <= n ; ++ j )
	  if( i < j )
	    {
	      
	      
	    }
      cout<<f[ 1 ][ n ]<<"\n";
    }
  return 0;
}
