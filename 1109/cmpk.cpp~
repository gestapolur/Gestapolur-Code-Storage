//hunan 2011 k direct cmpare
#include<iostream>
using namespace std;

int n , q ;
int base[ MAXN ];

void work()
{
  int i , j ;
  string ch;
  for( i = 1 ; i <= q ; ++ i )
    {
      cin>>ch;
      
    }
}

void init()
{
  cin>>n>>q;
  for( int i = 1 ; i <= n ; ++ i )
    cin>>base[ i ];
}

int main()
{
  init();
  
  work();

  return 0;
}
